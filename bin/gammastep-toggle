#!/usr/bin/env bash
set -euo pipefail

if pgrep -x gammastep >/dev/null 2>&1; then
    pkill -x gammastep
else
    nohup gammastep -m wayland -l 0:0 -g 1.10:1.10:1.10 -b 1.0:1.0 >/tmp/gammastep.log 2>&1 &
fi
