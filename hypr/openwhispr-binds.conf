# OpenWhispr keybinds (managed automatically)
# The Lua config contains the same bind; this file keeps the legacy config path working.
bind = CTRL, Super_L, exec, dbus-send --session --type=method_call --dest=com.openwhispr.App /com/openwhispr/App com.openwhispr.App.Toggle
